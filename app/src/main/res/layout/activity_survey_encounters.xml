<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SurveyEncountersActivity">

    <include layout="@layout/content_survey_encounters" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:orientation="vertical">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/btn_act_ses_add_se"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/fab_margin"
            android:onClick="addSurveyEncounter"
            app:srcCompat="@drawable/ic_add_item" />

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/btn_act_ses_add_multiple_ses"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/fab_margin"
            android:onClick="addMultipleSurveyEncounters"
            app:srcCompat="@drawable/ic_add_multiple_items" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>